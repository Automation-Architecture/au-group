[pytest]
pythonpath = .
asyncio_default_fixture_loop_scope = function
markers =
    integration: live API tests using .env, S3, and Supabase (run: pytest -m integration)
    smoke: dummy PDF API smoke tests for CI (no live S3/Supabase; run: pytest -m smoke)
