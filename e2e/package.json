{
  "name": "au-group-e2e",
  "private": true,
  "description": "Playwright E2E tests for AU Group (document-parser API + OpenAPI UI)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
